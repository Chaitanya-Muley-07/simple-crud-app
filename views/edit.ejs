<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/stylesheets/style.css">

</head>
<body>
    <div class="w-full h-full bg-zinc-900 text-white p-10">
      <div class="nav mb-10">
        <a class="text-blue-500" href="/read">Read</a>
      </div>
      <h1 class="text-tighter text-3xl" >Update User </h1>    
      <form action="/update/<%= user._id %>" method="post">
            <input class="px-5  py-2 border-2 bg-transparent border-zinc-800 rounded-lg" name="name" type="text" placeholder="name" value="<%= user.name %>" >
            <input class="px-5 py-2 border-2 bg-transparent border-zinc-800 rounded-lg" name="email"  type="text" placeholder="email" value="<%= user.email %>">
            <input class="px-5 py-2 border-2 bg-transparent border-zinc-800 rounded-lg" name="image" type="text" placeholder="image url" value="<%= user.image %> ">
            <input class="px-5 py-2 bg-yellow-500 rounded-lg text-black ml-3 " type="submit" value="Update-user">
          </form>
    </div>
</body>
</html>